/**
 * GrainOverlay — fixed 3% opacity noise texture over the entire viewport.
 * Placed once in the root layout; no props needed.
 */
export default function GrainOverlay() {
  return <div className="grain-overlay" aria-hidden="true" />;
}
